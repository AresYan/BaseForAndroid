<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.core.widget.SwipeRefreshLayout
        android:id="@+id/common_refresh_SwipeRefreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/common_refresh_RecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
    </androidx.core.widget.SwipeRefreshLayout>

    <include
        android:id="@+id/include_common_net_error"
        layout="@layout/common_net_error"
        android:visibility="gone"/>

    <include
        android:id="@+id/include_common_data_empty"
        layout="@layout/common_data_empty"
        android:visibility="gone"/>
</RelativeLayout>

